[project]
name = "hydra-wandb-broken"
version = "0.1.0"
description = "Fixture: canonical broken Hydra + W&B integration for testing /wire wandb-hydra"
requires-python = ">=3.11"
dependencies = [
  "hydra-core>=1.3",
  "omegaconf>=2.3",
  "wandb>=0.16",
]
